def rearrangeCharacters(s, target):
        
        res = []
        for i in range(len(target)):
            res.append(s.count(target[i])//target.count(target[i]))
            
        return min(res)

s = "ilovecodingonleetcode", target = "code"
print(rearrangeCharacters(s,target))